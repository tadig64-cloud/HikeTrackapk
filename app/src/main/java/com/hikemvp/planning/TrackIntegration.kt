package com.hikemvp.planning

import org.osmdroid.util.GeoPoint

// Stub minimal : remplace par ton vrai calcul si tu l'as.
fun computeTrackStats(points: List<GeoPoint>): Stats {
    return Stats(distanceM = 0.0, gainM = 0.0, lossM = 0.0)
}
