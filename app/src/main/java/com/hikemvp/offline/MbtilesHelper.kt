package com.hikemvp.offline

import android.content.Context
import org.osmdroid.views.MapView

/**
 * Stub minimal pour permettre la compilation.
 * TODO: Implémenter l'attache d'une source MBTiles via un TileProvider dédié.
 */
object MbtilesHelper {
    @JvmStatic
    fun attach(context: Context, mapView: MapView) {
        // no-op pour l'instant
    }
}